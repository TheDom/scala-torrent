
- once peer is identified, set PeerConnection's parent to Torrent instead of Coordinator
- accept Handshakes with extra bytes
- replace charset strings with StandardCharsets
- actor behaviors as separate classes